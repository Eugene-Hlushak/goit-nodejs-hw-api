const { isValidId } = require("./isValidId");
const {
  validateContactData,
  validateContactFavorite,
} = require("./validateContactData");

module.exports = {
  isValidId,
  validateContactData,
  validateContactFavorite,
};
