const { isValidId } = require("./isValidId");
const {
  validateContactData,
  validateContactFavorite,
  validateUserData,
} = require("./validateData");

module.exports = {
  isValidId,
  validateContactData,
  validateContactFavorite,
  validateUserData,
};
